import Layout from "@/layouts/defaultLayout";
import Wrapper from "@/layouts/Wrapper";

const ARPrints = () => {
  // empty page
  return (
    <Layout>
      <Wrapper>AR Prints</Wrapper>
    </Layout>
  );
};

export default ARPrints;
